import React from 'react';
import { SecretsAlignment } from '../types';
import { Shield, FileText, Zap, Link as LinkIcon, Lock, Search, RefreshCw, CheckCircle2 } from 'lucide-react';
import { motion } from 'motion/react';

interface SecretsAlignmentProps {
  data: SecretsAlignment;
}

const SecretsAlignmentView: React.FC<SecretsAlignmentProps> = ({ data }) => {
  return (
    <div className="space-y-8 p-6 bg-slate-950 border border-slate-800 rounded-xl font-mono text-slate-300">
      {/* HEADER: SECRETS ALIGNMENT */}
      <div className="flex items-center justify-between border-b border-slate-800 pb-4">
        <div className="flex items-center gap-3">
          <div className="p-2 bg-emerald-500/10 rounded-lg">
            <Lock className="w-6 h-6 text-emerald-500" />
          </div>
          <div>
            <h2 className="text-lg font-bold text-white uppercase tracking-tighter">Secrets Alignment [Ω]</h2>
            <p className="text-[10px] text-slate-500 italic">{data.selfIntegrityString}</p>
          </div>
        </div>
        <div className="flex items-center gap-2 px-3 py-1 bg-emerald-500/10 border border-emerald-500/20 rounded text-[10px] text-emerald-400 font-bold">
          <Shield className="w-3 h-3" /> INTEGRITY_VERIFIED
        </div>
      </div>

      {/* SYMMETRICAL ALIGNMENT GRID */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
        {/* LEFT: TRANSCRIPTION REPORT */}
        <div className="space-y-4">
          <h3 className="text-xs font-bold text-slate-500 flex items-center gap-2 uppercase tracking-widest">
            <FileText className="w-3 h-3 text-cyan-400" /> Transcription Report
          </h3>
          <div className="bg-slate-900 border border-slate-800 p-4 rounded-lg min-h-[200px] text-[11px] leading-relaxed text-slate-400">
            <p className="border-l-2 border-cyan-500 pl-4 py-1 bg-cyan-950/10 mb-4">
              {data.transcriptionReport}
            </p>
            <div className="space-y-2 opacity-60">
              <p>[METADEDUCTED_CONTENT_EVALUATION]: Visual content evaluation active.</p>
              <p>[STATUS]: Showcase available for forensic audit.</p>
            </div>
          </div>
        </div>

        {/* RIGHT: WHY-TO-FUNCTION REASON */}
        <div className="space-y-4">
          <h3 className="text-xs font-bold text-slate-500 flex items-center gap-2 uppercase tracking-widest">
            <Zap className="w-3 h-3 text-amber-400" /> Why-to-Function Reason
          </h3>
          <div className="bg-slate-900 border border-slate-800 p-4 rounded-lg min-h-[200px] text-[11px] leading-relaxed text-slate-400">
            <div className="p-3 bg-amber-500/5 border border-amber-500/20 rounded italic text-amber-200/80">
              {data.whyToFunctionReason}
            </div>
            <div className="mt-4 space-y-2 text-[10px] font-mono text-slate-500">
              <p className="flex items-center gap-2"><CheckCircle2 className="w-3 h-3 text-emerald-500" /> Static Symmetrical Alignment Code Enforced</p>
              <p className="flex items-center gap-2"><CheckCircle2 className="w-3 h-3 text-emerald-500" /> Concept-Framework-Set Deducted</p>
            </div>
          </div>
        </div>
      </div>

      {/* PDF-TO-PDF ANALYSIS & SYNCING */}
      <div className="bg-slate-900 border border-slate-800 rounded-lg overflow-hidden">
        <div className="bg-slate-800/50 px-4 py-3 border-b border-slate-800 flex items-center justify-between">
          <h3 className="text-xs font-bold text-slate-200 flex items-center gap-2">
            <Search className="w-4 h-4 text-purple-400" /> PDF-TO-PDF ANALYSIS [INSPECTVIEW]
          </h3>
          <div className="flex gap-2">
            <button className="px-3 py-1 bg-cyan-600 hover:bg-cyan-500 text-white text-[10px] font-bold rounded transition-colors flex items-center gap-2">
              <FileText className="w-3 h-3" /> PRESS_REPORT
            </button>
            <button className="px-3 py-1 bg-purple-600 hover:bg-purple-500 text-white text-[10px] font-bold rounded transition-colors flex items-center gap-2">
              <FileText className="w-3 h-3" /> PDF_EXPORT
            </button>
          </div>
        </div>
        <div className="p-6 grid grid-cols-1 md:grid-cols-3 gap-6">
          <div className="space-y-2">
            <span className="text-[10px] text-slate-500 uppercase font-bold">Input Hash</span>
            <code className="block p-2 bg-black rounded text-[10px] text-cyan-500 truncate">{data.pdfAnalysis.inputHash}</code>
          </div>
          <div className="space-y-2">
            <span className="text-[10px] text-slate-500 uppercase font-bold">Output Hash</span>
            <code className="block p-2 bg-black rounded text-[10px] text-purple-500 truncate">{data.pdfAnalysis.outputHash}</code>
          </div>
          <div className="space-y-2">
            <span className="text-[10px] text-slate-500 uppercase font-bold">Alignment Status</span>
            <div className={`flex items-center gap-2 p-2 rounded text-[10px] font-bold ${
              data.pdfAnalysis.alignmentStatus === 'SYNCED' ? 'bg-emerald-500/10 text-emerald-500' : 'bg-red-500/10 text-red-500'
            }`}>
              <RefreshCw className={`w-3 h-3 ${data.pdfAnalysis.alignmentStatus === 'SYNCED' ? '' : 'animate-spin'}`} />
              {data.pdfAnalysis.alignmentStatus}
            </div>
          </div>
        </div>
        <div className="px-6 pb-6 text-[10px] text-slate-500 italic">
          Reasoning: Syncing rules applied to ensure visual string PDF corrected alignment-linked press and pdf buttons are correctly synced.
        </div>
      </div>

      {/* TERMINOLOGY ANALYSIS UPGRADES */}
      <div className="space-y-4">
        <h3 className="text-xs font-bold text-slate-500 flex items-center gap-2 uppercase tracking-widest">
          <RefreshCw className="w-3 h-3 text-blue-400" /> Terminology Analysis Upgrades
        </h3>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
          {data.terminologyUpgrades.map((upgrade, i) => (
            <div key={i} className="bg-slate-900 border border-slate-800 p-4 rounded-lg space-y-3">
              <div className="flex justify-between items-center">
                <span className="text-[10px] font-bold text-red-400 line-through opacity-50">{upgrade.originalTerm}</span>
                <ChevronRight className="w-3 h-3 text-slate-600" />
                <span className="text-[10px] font-bold text-emerald-400">{upgrade.upgradedTerm}</span>
              </div>
              <p className="text-[11px] text-slate-400 leading-tight">{upgrade.reasoning}</p>
              <div className="w-full bg-slate-800 h-1 rounded-full overflow-hidden">
                <div className="bg-blue-500 h-full" style={{ width: `${upgrade.alignmentScore * 100}%` }} />
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
};

const ChevronRight = ({ className }: { className?: string }) => (
  <svg className={className} fill="none" viewBox="0 0 24 24" stroke="currentColor">
    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="9 5l7 7-7 7" />
  </svg>
);

export default SecretsAlignmentView;
